# Import a LightLDAP service by specifying the Project ID it belongs to, and the LightLDAP service ID (spaced by a comma).
terraform import elestio_lightldap.my_lightldap project_id,lightldap_service_id
