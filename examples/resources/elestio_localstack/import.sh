# Import a LocalStack service by specifying the Project ID it belongs to, and the LocalStack service ID (spaced by a comma).
terraform import elestio_localstack.my_localstack project_id,localstack_service_id
