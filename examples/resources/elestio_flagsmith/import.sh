# Import a Flagsmith service by specifying the Project ID it belongs to, and the Flagsmith service ID (spaced by a comma).
terraform import elestio_flagsmith.my_flagsmith project_id,flagsmith_service_id
