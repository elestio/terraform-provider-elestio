# Import a Attendize service by specifying the Project ID it belongs to, and the Attendize service ID (spaced by a comma).
terraform import elestio_attendize.my_attendize project_id,attendize_service_id
