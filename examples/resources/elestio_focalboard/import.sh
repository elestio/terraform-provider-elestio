# Import a FocalBoard service by specifying the Project ID it belongs to, and the FocalBoard service ID (spaced by a comma).
terraform import elestio_focalboard.my_focalboard project_id,focalboard_service_id
