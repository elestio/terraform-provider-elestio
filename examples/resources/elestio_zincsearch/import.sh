# Import a ZincSearch service by specifying the Project ID it belongs to, and the ZincSearch service ID (spaced by a comma).
terraform import elestio_zincsearch.my_zincsearch project_id,zincsearch_service_id
