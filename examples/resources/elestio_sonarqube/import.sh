# Import a SonarQube service by specifying the Project ID it belongs to, and the SonarQube service ID (spaced by a comma).
terraform import elestio_sonarqube.my_sonarqube project_id,sonarqube_service_id
