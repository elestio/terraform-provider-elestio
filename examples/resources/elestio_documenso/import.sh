# Import a Documenso service by specifying the Project ID it belongs to, and the Documenso service ID (spaced by a comma).
terraform import elestio_documenso.my_documenso project_id,documenso_service_id
