# Import a Streamlit service by specifying the Project ID it belongs to, and the Streamlit service ID (spaced by a comma).
terraform import elestio_streamlit.my_streamlit project_id,streamlit_service_id
