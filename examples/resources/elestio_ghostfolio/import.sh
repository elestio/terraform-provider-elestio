# Import a Ghostfolio service by specifying the Project ID it belongs to, and the Ghostfolio service ID (spaced by a comma).
terraform import elestio_ghostfolio.my_ghostfolio project_id,ghostfolio_service_id
