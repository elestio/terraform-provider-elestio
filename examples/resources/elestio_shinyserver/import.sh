# Import a ShinyServer service by specifying the Project ID it belongs to, and the ShinyServer service ID (spaced by a comma).
terraform import elestio_shinyserver.my_shinyserver project_id,shinyserver_service_id
