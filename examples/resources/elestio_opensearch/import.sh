# Import a OpenSearch service by specifying the Project ID it belongs to, and the OpenSearch service ID (spaced by a comma).
terraform import elestio_opensearch.my_opensearch project_id,opensearch_service_id
