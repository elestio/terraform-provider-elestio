# Import a OVA-Runner service by specifying the Project ID it belongs to, and the OVA-Runner service ID (spaced by a comma).
terraform import elestio_ova_runner.my_ova_runner project_id,ova_runner_service_id
