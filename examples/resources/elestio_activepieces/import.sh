# Import a Activepieces service by specifying the Project ID it belongs to, and the Activepieces service ID (spaced by a comma).
terraform import elestio_activepieces.my_activepieces project_id,activepieces_service_id
