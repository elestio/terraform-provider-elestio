# Import a Airflow-worker service by specifying the Project ID it belongs to, and the Airflow-worker service ID (spaced by a comma).
terraform import elestio_airflow_worker.my_airflow_worker project_id,airflow_worker_service_id
