# Import a Jupyter Notebook service by specifying the Project ID it belongs to, and the Jupyter Notebook service ID (spaced by a comma).
terraform import elestio_jupyter.my_jupyter project_id,jupyter_service_id
