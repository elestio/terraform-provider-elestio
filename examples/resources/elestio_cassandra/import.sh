# Import a Cassandra service by specifying the Project ID it belongs to, and the Cassandra service ID (spaced by a comma).
terraform import elestio_cassandra.my_cassandra project_id,cassandra_service_id
