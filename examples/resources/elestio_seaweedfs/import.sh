# Import a SeaweedFS service by specifying the Project ID it belongs to, and the SeaweedFS service ID (spaced by a comma).
terraform import elestio_seaweedfs.my_seaweedfs project_id,seaweedfs_service_id
