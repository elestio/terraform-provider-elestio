# Import a ColumnStore service by specifying the Project ID it belongs to, and the ColumnStore service ID (spaced by a comma).
terraform import elestio_columnstore.my_columnstore project_id,columnstore_service_id
