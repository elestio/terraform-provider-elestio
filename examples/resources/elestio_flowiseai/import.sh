# Import a FlowiseAI service by specifying the Project ID it belongs to, and the FlowiseAI service ID (spaced by a comma).
terraform import elestio_flowiseai.my_flowiseai project_id,flowiseai_service_id
