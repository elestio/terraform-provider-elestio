# Import a Password Pusher service by specifying the Project ID it belongs to, and the Password Pusher service ID (spaced by a comma).
terraform import elestio_password.my_password project_id,password_service_id
