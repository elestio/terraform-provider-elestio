# Import a Linux-desktop service by specifying the Project ID it belongs to, and the Linux-desktop service ID (spaced by a comma).
terraform import elestio_linux_desktop.my_linux_desktop project_id,linux_desktop_service_id
