# Import a CI-CD-Target service by specifying the Project ID it belongs to, and the CI-CD-Target service ID (spaced by a comma).
terraform import elestio_ci_cd_target.my_ci_cd_target project_id,ci_cd_target_service_id
