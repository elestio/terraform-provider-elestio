# Import a Prometheus service by specifying the Project ID it belongs to, and the Prometheus service ID (spaced by a comma).
terraform import elestio_prometheus.my_prometheus project_id,prometheus_service_id
