# Import a DockerRegistry service by specifying the Project ID it belongs to, and the DockerRegistry service ID (spaced by a comma).
terraform import elestio_dockerregistry.my_dockerregistry project_id,dockerregistry_service_id
