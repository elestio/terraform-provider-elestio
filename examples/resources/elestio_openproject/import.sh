# Import a OpenProject service by specifying the Project ID it belongs to, and the OpenProject service ID (spaced by a comma).
terraform import elestio_openproject.my_openproject project_id,openproject_service_id
