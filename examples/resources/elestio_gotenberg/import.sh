# Import a Gotenberg service by specifying the Project ID it belongs to, and the Gotenberg service ID (spaced by a comma).
terraform import elestio_gotenberg.my_gotenberg project_id,gotenberg_service_id
