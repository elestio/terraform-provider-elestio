# Import a Authentik service by specifying the Project ID it belongs to, and the Authentik service ID (spaced by a comma).
terraform import elestio_authentik.my_authentik project_id,authentik_service_id
