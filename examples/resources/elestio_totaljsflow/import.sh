# Import a TotaljsFlow service by specifying the Project ID it belongs to, and the TotaljsFlow service ID (spaced by a comma).
terraform import elestio_totaljsflow.my_totaljsflow project_id,totaljsflow_service_id
