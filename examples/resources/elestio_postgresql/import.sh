# Import a PostgreSQL service by specifying the Project ID it belongs to, and the PostgreSQL service ID (spaced by a comma).
terraform import elestio_postgresql.my_postgresql project_id,postgresql_service_id
