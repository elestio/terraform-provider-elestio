# Import a Gitlab-runner service by specifying the Project ID it belongs to, and the Gitlab-runner service ID (spaced by a comma).
terraform import elestio_gitlab_runner.my_gitlab_runner project_id,gitlab_runner_service_id
