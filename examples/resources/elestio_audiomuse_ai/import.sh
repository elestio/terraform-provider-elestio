# Import a AudioMuse-AI service by specifying the Project ID it belongs to, and the AudioMuse-AI service ID (spaced by a comma).
terraform import elestio_audiomuse_ai.my_audiomuse_ai project_id,audiomuse_ai_service_id
